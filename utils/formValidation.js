const formValidation = (requiredSlots, domainSlots, customSlots) => {
  // requiredSlots: what the form needs
  // domainSlots: slots specified in the domain
  // customSlots: must be mapped a different way
  const resPayload = { events: [], responses: [] };
  return new Promise((resolve, reject) => {
    // text of last user input
    const extractGoal = () => {};
  });
};
